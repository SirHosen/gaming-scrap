# Development & CI tooling (pinned for reproducible builds).
# Runtime deps live in requirements.txt / pyproject.toml.
#
#   pip install -r requirements-dev.txt

pytest==8.2.2
ruff==0.5.6
mypy==1.11.1
types-requests==2.32.0.20240712

# Optional runtime extras exercised by the test-suite / CI:
aiohttp>=3.9.0      # async client path
pyyaml>=6.0         # YAML config loader
